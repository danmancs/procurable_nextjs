// app/layout.tsx
import { ReactNode } from 'react';
import { setup } from 'twind';
import tailwindConfig from '../tailwind.config'; // Assuming twind config is in the root

import { Metadata } from 'next'
 
export const metadata: Metadata = {
  title: 'Procurable Group',
  description: 'Project Management and Marine Infrastructure',
}
